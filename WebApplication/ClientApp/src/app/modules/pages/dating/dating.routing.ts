import { Route } from '@angular/router';
import { DatingComponent } from './dating.component'

export const datingRoutes: Route[] = [
    {
        path     : '',
        component: DatingComponent
    }
];
